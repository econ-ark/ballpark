#!/bin/sh
# Install dependencies with uv (must match pyproject.toml)
uv pip install --system econ-ark jupyter jupyterlab jupyterlab-citation-manager

# Notebooks using cite2c format: jupyterlab-citation-manager will offer to
# migrate them on first open in JupyterLab.
