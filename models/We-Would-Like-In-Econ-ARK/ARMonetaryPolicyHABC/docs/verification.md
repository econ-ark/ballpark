# Verification (Matsya / AI iteration vs. the paper)

I accepted Matsya's two-stage decomposition — **decision** followed by **shock realization** — because it matches the max-then-expectation structure in the household Bellman statement summarized in `ToramanSY_AlganRagot2009_Summary.ipynb` and keeps optimization separate from the Markov update on productivity. I edited the draft YAML to make assumptions explicit where the ballpark summary is thin: the borrowing limit (`a_min`) and the law of motion for next-period resources (`q_next` in the shock stage) are flagged as unresolved; the Markov kernel was renamed from `Pi` to `P_e` to remove a collision with the inflation series `Pi_inf` on the aggregate side; the transfer `mu` was moved from `R+` to `R` to accommodate the inflation-tax redistribution channel; and the identity transitions in both stages are now labelled as degenerate so reviewers can distinguish "identity" from "omitted." I rejected treating the separable money-in-utility form in `docs/dolo-plus-draft.yaml` as paper-identical: the summary's Bellman excerpt records a **CES composite over consumption and money with leisure** parameterized by `(sigma, eta, omega, psi)` (now in the `docs/bellman-excerpt.md` symbol table), so the current YAML remains a **computational scaffold** with explicit `# workaround:` and `# unresolved:` flags. **Note on paper-grounding:** CONTRIBUTING.md asks that verification compare against the **published paper**, not only the ballpark summary; the paper PDF / `.mmd` is not yet in this item directory, so every accept / edit / reject claim above is currently grounded in the summary notebook and the `docs/arg2009-*.md` excerpts only. Replacing the paragraph with one that cites specific equation numbers from Algan–Ragot (2009) is tracked as deferred item #2 in `docs/accept-edit-reject.md` and is the single most-important verification upgrade outstanding.
